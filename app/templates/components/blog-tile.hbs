<div>
  <h3>{{#link-to 'blog-detail' blog.id}}{{blog.title}}{{/link-to}}</h3>
  <h4>{{blog.date}}</h4>
  <p>{{blog.body}}</p>
  {{#if admin}}
    <button {{action 'deleteMe' blog}}>Delete</button>
    <button {{action 'updateMe' blog}}>Update</button>
  {{/if}}
</div>
