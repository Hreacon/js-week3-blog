<div>
  <h3>{{blog.title}}</h3>
  <h4>{{blog.date}}</h4>
  <p>{{blog.body}}</p>
  {{#if admin}}
    <button {{action 'deleteMe' blog}}>Delete</button>
    <button {{action 'updateMe' blog}}>Update</button>
  {{/if}}
</div>
